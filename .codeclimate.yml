exclude_patterns:
  - "**/migrations/"
  - "**/tests/"
  
  plugins:
  duplication:
    enabled: true
    config:
      languages:
        python:
          python_version: 3
          mass_threshold: 150
